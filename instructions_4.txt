## ✍️ High‑Level Prompt for the AI

```
You are an expert in real‑time speech systems, the WhisperX ecosystem,
FastAPI, and the Resemble AI / Meta NLLB model families. Our server
(`server.py`) has a new `/ws/s2s` endpoint that immediately disconnects
clients. Three root causes must be fixed:

## Symptoms
1. Client sees "Disconnected" immediately after clicking Start.
2. RunPod logs show no WebSocket errors except the disconnect.
3. The `/health` endpoint returns `"s2s_enabled": false`.
4. Server log contains: "Could not load library libcudnn_ops_infer.so.8"

## Root Causes
A. `S2S_ENABLED` defaults to `"0"` (disabled). It must be `1` on RunPod.
B. The `/ws/s2s` handler imports `from s2s import translator` and
   `from s2s import tts`, but the `s2s/` directory and those Python
   modules do not exist. The import fails silently or crashes the
   WebSocket.
C. The cuDNN 8 library is missing from the CUDA 12.4 Docker image,
   causing a non‑fatal but confusing warning.

## Required Deliverables

### 1. Create the missing `s2s/` Python package
Write three new files:

#### `s2s/__init__.py`
Empty file (just a package marker).

#### `s2s/translator.py`
A singleton module that:
- Lazily loads `facebook/nllb-200-distilled-600M` via
  `transformers.AutoModelForSeq2SeqLM` and `AutoTokenizer`.
- Caches the model globally after first load (like ModelRegistry).
- Provides a `translate(text, source_lang, target_lang)` function that:
  * Converts ISO 639‑1 codes (e.g. "en", "fr") to NLLB‑200's
    FLORES‑200 codes (e.g. "eng_Latn", "fra_Latn").
  * Uses `tokenizer(src_lang=..., tgt_lang=...)` and
    `model.generate(forced_bos_token_id=tokenizer.lang_code_to_id[tgt_lang])`.
  * Returns the translated string.
- Provides a `warmup()` method that runs a dummy translation to pre‑load
  CUDA kernels.
- Exposes `iso1_to_flores()` for language code validation.

#### `s2s/tts.py`
A singleton module that:
- Lazily loads `ResembleAI/chatterbox-turbo` via
  `chatterbox.tts_turbo.ChatterboxTurboTTS.from_pretrained(device="cuda")`.
- Caches the model globally.
- Provides a `synthesize(text)` method that calls
  `model.generate(text, audio_prompt_path=REF_CLIP)` where `REF_CLIP`
  is a bundled 10‑second WAV file (e.g. `static/ref_speaker.wav`) that
  ships with the Docker image. If the file is missing, fall back to
  `model.generate(text)` without a reference clip.
- `synthesize()` returns a `torch.Tensor` of float32 audio samples at
  the model's native sample rate (24 kHz).
- Exposes `TTS_BACKEND = "chatterbox-turbo"`, `language = "en"`, and
  `sample_rate = 24000` as module‑level constants.
- Provides `is_available()` and `availability_error()`.
- Provides a `warmup()` method that runs a dummy synthesis.

### 2. Fix the cuDNN library warning
In the Dockerfile, add the following line **before** the pip install
step:

```dockerfile
RUN apt-get update && apt-get install -y --no-install-recommends \
    libcudnn8 libcudnn8-dev && \
    rm -rf /var/lib/apt/lists/*
```

Also add this environment variable so CTranslate2 finds the library:

```dockerfile
ENV LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH
```

### 3. Update `requirements.txt`
Ensure these packages are pinned:
```
chatterbox-tts>=0.1.0
transformers>=4.45.0
sentencepiece>=0.2.0
protobuf>=4.25.0
```

### 4. Update `server.py`
Make one small change to the lifespan handler: if `S2S_ENABLED` is true
but the `s2s` package cannot be imported, log a clear error and set
`s2s_import_error` on the registry so `/health` reports it. Do NOT
let the server crash.

```python
if S2S_ENABLED:
    try:
        from s2s import translator as _s2s_translator
        from s2s import tts as _s2s_tts
        logger.info("S2S module loaded — TTS backend %s", _s2s_tts.TTS_BACKEND)
    except Exception as exc:
        logger.error("S2S module failed to import: %s", exc)
        S2S_ENABLED = False  # prevent /ws/s2s from accepting connections
```

### 5. Update the Web UI (`static/index.html`)
If the Speech‑to‑Speech tab is present, ensure that when the user
clicks Start, it displays a clear error message if the server returns
an error JSON (instead of just "Disconnected").

### 6. Deployment checklist
Write a `S2S_DEPLOY.md` file with:
- How to set `S2S_ENABLED=1` on RunPod.
- How to accept the Chatterbox‑Turbo license on huggingface.co.
- How to bundle a reference voice clip (`static/ref_speaker.wav`).
- A simple `curl`‑based test that connects to `/ws/s2s` with a
  WebSocket client and verifies the "ready" message.

## Verification
After deploying, the following must work:
1. `curl /health` returns `"s2s_enabled": true` and `"s2s_tts_backend": "chatterbox-turbo"`.
2. A WebSocket connection to `/ws/s2s` receives `{"type":"ready"}`.
3. Speaking into the microphone produces a transcription, a translation,
   and (if target is English) synthesised audio.

Write all the files described above. Keep the existing `server.py`
endpoints (`/transcribe`, `/align`, `/session/diarize`, `/ws/stt`)
completely untouched. The only changes to `server.py` are the
S2S‑import guard in the lifespan handler and the `S2S_ENABLED`
default.
```

This prompt creates everything that's missing and fixes the silent failure. Give it to your AI, and once the files are generated, set `S2S_ENABLED=1` on RunPod, redeploy, and test again.